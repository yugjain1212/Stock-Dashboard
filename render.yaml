services:
  - type: web
    name: stock-dashboard
    env: python
    buildCommand: pip install -r requirements.txt && python -m app.services.fetcher
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./stocks.db